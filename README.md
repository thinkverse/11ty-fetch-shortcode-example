# Using node-fetch in shortcodes

This repository contains an example showcasing using `node-fetch` in eleventy shortcodes.

## Usage

1. Clone repository
2. Install dependencies.
3. Run using `npm start`
